function toggleMode() {
    const html = document.documentElement

     //if(html.classList.contains('light')) {
     //   html.classList.remove('light') 
     // } else {
     //   html.classList.add('light')}
  
     //código pode ser simplificado com o toggle já existente

     html.classList.toggle('light')
}
    